# Packages tests

Unit tests for shared packages (ui, logging, auth, utils).

- **Library/component tests** — Per package (e.g. `packages/ui/tests/` or co-located `*.test.tsx`).

Use the standard tool for the stack (e.g. Vitest, Jest).  
See [docs/TESTING.md](../../docs/TESTING.md) in the parent repo.
